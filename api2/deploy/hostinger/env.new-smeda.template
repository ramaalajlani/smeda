# new.smeda.gov.sy — إعدادات .env

APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:YOUR_APP_KEY_HERE

# Laravel public folder: public_html/api2/public
APP_URL=https://new.smeda.gov.sy/api2/public
BACKEND_URL=https://new.smeda.gov.sy/api2/public
FRONTEND_URL=https://new.smeda.gov.sy

SESSION_DRIVER=file
SESSION_PATH=/
SESSION_DOMAIN=.smeda.gov.sy

SANCTUM_STATEFUL_DOMAINS=new.smeda.gov.sy
CORS_ALLOWED_ORIGINS=https://new.smeda.gov.sy

# بعد الرفع:
# php artisan config:clear && php artisan route:clear && php artisan view:clear
# chmod -R 775 storage bootstrap/cache
